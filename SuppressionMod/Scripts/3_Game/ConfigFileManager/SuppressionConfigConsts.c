static const string SUPPRESSION_ROOT_DIR = "$profile:Suppression/";

static const string SUPPRESSION_EARPROTECTION_CONFIG = SUPPRESSION_ROOT_DIR + "EarProtectionItems.txt";

//Not needed | File was working server side we just werent passing info from server to client
//static const string SUPPRESSION_EARPROTECTION_CONFIG = SUPPRESSION_ROOT_DIR + SUPPRESSION_EARPROTECTION_TXT;